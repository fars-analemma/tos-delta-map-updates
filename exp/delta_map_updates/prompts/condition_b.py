# Condition B: Full regen + preserve/overwrite rules prompt.
# The model is given M_{t-1} and O_t, and must output a full updated map M_t,
# with explicit rules to preserve unchanged entries and overwrite contradicted entries.
